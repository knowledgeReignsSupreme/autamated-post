import React from 'react';
import styled from 'styled-components';

const Home: React.FC = () => {
  return (
    <StyledHome>
      <h2>Welcome to the Israel Post app</h2>
    </StyledHome>
  );
};

const StyledHome = styled.main``;

export default Home;
